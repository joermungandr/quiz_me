<h1>Editing quiz</h1>

<%= form_for(@quiz) do |f| %>
        <div>
          <%= f.label :name, ("Quizname") %><br>
          <%= f.text_area  :name  %>
        </div>
        <div>             
          <%= f.label :questions, ("Fragen auswählen") %><br>
          <%= f.collection_select(:questions, Question.all, :id, :description,{:include_hidden => false},{:multiple => true}) %>
        </div>
        <br>
        <div>
          <%= f.submit "Quiz bearbeiten", class: "btn btn-large btn-primary" %>
        </div>
 <% end %>

<%= link_to 'Show', @quiz %> |
<%= link_to 'Back', quizzes_path %>
